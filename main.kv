FloatLayout:
  TabbedPanel:
    size_hint: 1, 1
    pos_hint: { 'center_x': .5, 'center_y': .5 }
    do_default_tab: False

    TabbedPanelItem:
      text: 'Courses'

      CoursesTab:

    TabbedPanelItem:
      text: 'Groups'

      GroupsTab:

    TabbedPanelItem:
      text: 'Rooms'

    TabbedPanelItem:
      text: 'Generate\nTimetable'
      size_hint_x: 5

      BoxLayout:
        orientation: 'vertical'

        Button:
          text: 'Generate Table'
          size_hint_y: 1
          on_press: app.generate_table()

        Widget:
          size_hint_y: 10
